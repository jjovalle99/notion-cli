name: Publish to PyPI

on:
  release:
    types: [created]

jobs:
  publish:
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/notion-cli
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v5
        with:
          version: "latest"

      - name: Inject OAuth secret
        run: echo "CLIENT_SECRET = \"${{ secrets.NOTION_OAUTH_CLIENT_SECRET }}\"" > src/notion_cli/_oauth_secret.py

      - name: Build distribution
        run: uv build

      - name: Publish to PyPI
        run: uv publish
